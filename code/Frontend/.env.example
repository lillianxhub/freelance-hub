# Supabase Project URL from Project Settings > API
VITE_SUPABASE_URL=

# Browser-safe publishable key (or legacy anon key). Never use service_role here.
VITE_SUPABASE_PUBLISHABLE_KEY=

# true forces local demo data even when Supabase credentials exist.
VITE_DEMO_MODE=true
